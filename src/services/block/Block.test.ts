import chai from 'chai';

describe("test block", function () {
  describe("just test", function () {
    it("складываем 2 числа внутри block", function () {
      chai.assert.equal(1, 1);
    });
  });
});
