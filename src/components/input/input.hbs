<input
  class="input{{#if disabled}} disabled{{/if}}
    {{#if reverseAlign}}input__rigth-align{{/if}}"
  {{#if placeholder}} placeholder="{{placeholder}}" {{/if}}
  {{#if type}} type="{{type}}" {{else}}type="text" {{/if}}
  {{#if id}} id="{{id}}" {{/if}}
  {{#if name}} name="{{name}}" {{/if}}
  {{#if value}} value="{{value}}" {{/if}}
  autocomplete="off"
/>